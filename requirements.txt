# ============================================================
# RetailVision Pilot — Dependencias base (CPU)
# ============================================================
# Para GPU (CUDA): usa requirements-cuda.txt en vez de este.
#   pip install -r requirements-cuda.txt
#
# Para CPU (cualquier PC):
#   pip install -r requirements.txt
#   python app.py
# ============================================================

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.4
python-multipart==0.0.19
aiofiles==24.1.0

# Computer vision
opencv-python-headless==4.10.0.84
numpy>=2.0.0

# YOLO + PyTorch (CPU version, automático desde PyPI)
ultralytics==8.3.61
